import { NextRequest } from 'next/server'; import { requireEditorOrAbove,handleApiError,apiResponse } from '@/pipeline/middleware/auth.middleware'; import ArticleService from '@/pipeline/articles/article.service';
export async function POST(req:NextRequest,{params}:{params:Promise<{id:string}>}){try{await requireEditorOrAbove(req);const {id}=await params;const body=await req.json() as {version:number};if(!Number.isInteger(body.version))throw new Error('version is required');await ArticleService.rollback(id,body.version);return apiResponse({message:'Article rolled back'})}catch(e){return handleApiError(e)}}
